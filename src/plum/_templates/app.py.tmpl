from __future__ import annotations

from plum import autodiscover, build_cli

from . import pipelines
from .catalog import CATALOG
from .registries import PIPELINES

# imports every module in pipelines/, running their registration decorators
autodiscover(pipelines)

app = build_cli(catalog=CATALOG, pipelines=PIPELINES)

if __name__ == "__main__":
    app()
